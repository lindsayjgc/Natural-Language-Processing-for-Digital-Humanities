# Core
pandas
numpy
nltk

# NLP
spacy
transformers
torch
sentencepiece   # required by many transformer tokenizers

# Document I/O (optional but recommended for .docx/.pdf/.rtf support)
docx2txt
python-docx
pypdf  # Required for PDFs

# API
fastapi
uvicorn[standard]
python-multipart
pymongo
motor
python-dotenv

# Authentication
python-jose[cryptography]
passlib[bcrypt]
bcrypt<4.0.0  # Required for passlib compatibility
python-multipart

# Utility / CLI
tqdm             # for progress display (optional but useful)
